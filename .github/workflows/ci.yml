name: CI - Pruebas y Despliegue en Render

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  tests-backend:
    name: Tests Backend (pytest)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout codigo
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          cd backend
          pip install fastapi uvicorn pytest pytest-mock httpx pydantic

      - name: Ejecutar todos los tests
        run: |
          cd backend
          pytest tests/ -v --tb=short

  tests-e2e:
    name: Tests E2E (Playwright)
    runs-on: ubuntu-latest
    needs: tests-backend

    steps:
      - name: Checkout codigo
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependencias frontend
        run: |
          cd frontend
          npm install

      - name: Instalar Playwright
        run: |
          cd frontend
          npx playwright install --with-deps chromium

      - name: Arrancar backend en background
        run: |
          pip install fastapi uvicorn pydantic
          cd backend
          uvicorn main:app --host 0.0.0.0 --port 8000 &
          sleep 8

      - name: Verificar que el backend responde
        run: curl http://localhost:8000/

      - name: Ejecutar pruebas E2E
        run: |
          cd frontend
          npx playwright test --config ../playwright.config.js --reporter=list

  deploy-render:
    name: Desplegar en Render
    runs-on: ubuntu-latest
    needs: [tests-backend, tests-e2e]

    steps:
      - name: Trigger deploy en Render
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
          echo "Despliegue en Render correcto"